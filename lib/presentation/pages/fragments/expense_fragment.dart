import 'package:flutter/material.dart';

class ExpenseFragment extends StatelessWidget {
  const ExpenseFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Expense'),
    );
  }
}
